name: terraform-s3-image-thumbnails-generator
bitbucket_repo: terraform-s3-image-thumbnails-generator
badges: []
related: []
# description: ""
usage: |-

  Terraform:

  ```hcl
  module "s3-image-resizer" {
    source = "git::ssh://git@bitbucket.org/miquido/miquido-node-image-resizer-lambda?ref=master"

    namespace = "${var.project}"
    stage     = "${var.environment}"
  }
  ```
include:
  - "docs/terraform.md"
  - "docs/targets.md"
contributors:
  - name: "Paweł Jędruch"
    github: "pawcik"

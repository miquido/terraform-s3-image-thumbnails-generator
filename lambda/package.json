{
  "name": "image-resizer",
  "version": "1.0.0",
  "description": "Lambda to generate s3 thumbnails",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "sharp": "0.23.0"
  }
}

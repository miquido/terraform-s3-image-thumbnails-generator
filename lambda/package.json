{
  "name": "image-resizer",
  "version": "1.0.0",
  "description": "Lambda to generate s3 thumbnails",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "heic-convert": "^1.2.4",
    "sharp": "^0.31.2"
  },
  "devDependencies": {
    "serverless-offline": "^11.6.0"
  }
}
